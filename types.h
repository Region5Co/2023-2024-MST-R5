enum class drivetrain {
  mecanum,
  twoWheel
};

enum class turnDirection {
  cw,
  ccw
};

enum class moveDirection {
  forward,
  backward,
  left,
  right
};

enum class WHEEL{
  FRONT_LEFT,
  FRONT_RIGHT,
  BACK_LEFT,
  BACK_RIGHT
};